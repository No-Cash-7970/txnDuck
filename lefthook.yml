pre-commit:
  follow: true
  commands:
    precommit-gulp:
      run: yarn gulp precommitHook
      exclude:
        - "*.md"
commit-msg:
  follow: true
  commands:
    commitlint:
      run: yarn commitlint --edit
